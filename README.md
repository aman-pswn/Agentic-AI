An experimental Generative AI notebook showcasing model implementation, prompt handling, and AI-generated outputs using Python.
with the help of dictionary Keyword 
A dictionary stores keywords and their corresponding responses.

The program checks user input.

If a keyword matches, it returns the predefined response.

If no keyword matches, it gives a default reply.
